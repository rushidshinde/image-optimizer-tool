import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    template: "%s | Responsive image audit tool",
    default: "Dashboard | Responsive image audit tool",
  },
  description: "A powerful suite of tools to audit, analyze, and optimize your web images for maximum performance and visual quality.",
  keywords: ["image optimizer", "web performance", "SEO", "image auditing", "image resizer", "responsive images"],
  authors: [{ name: "Developer" }],
  viewport: "width=device-width, initial-scale=1, maximum-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
